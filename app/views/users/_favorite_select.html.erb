<%= form_for(current_user.favorite_lists.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :favo_id, fav.id %></div>
    <%= f.submit "Favorite", class: "btn btn-success btn-sm" %>
<% end %>